import { SlotDirective } from './slot.directive';

describe('SlotDirective', () => {
  it('should create an instance', () => {
    const directive = new SlotDirective();
    expect(directive).toBeTruthy();
  });
});
